{
  "name": "your-project-name",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    // 您的依赖项
  },
  "devDependencies": {
    // 开发依赖项
  }
}
